// Placeholder for geometry utility functions
// This file will contain helper functions for procedural tree generation
// and other geometric calculations.

export const degToRad = (degrees: number): number => {
  return degrees * (Math.PI / 180);
};